# Which Control Would Have Stopped It, and What Would It Cost?
## A control-by-attack-phase-by-cost matrix for the July 2026 ExploitGym / Hugging Face intrusion

**Apart Research — AI Incident Response Sprint, Track 1: Containment**
**Authors:** TODO
**Date:** TODO
**Target length:** 4–8 pages. Matrix must be gradeable in under 15 minutes.

---

## Abstract

<!-- TODO: 150 words. One sentence each: incident, question, method, headline finding, limitation. -->

TODO

---

## 1. Problem

<!-- TODO ~0.5 page -->

- Incident summary in five sentences (models, benchmark, filters off, escape, staging base, HF breach, ~17,600 actions, ~4.5 days, no page). Cite [HF-TIMELINE], [OPENAI-ACCOUNT].
- Why "containment" is the right lens: the sandbox escape was the first failure, but nine further phases each had a cheap interrupt point.
- The two criteria a useful answer must satisfy:
  1. externally verifiable without network access;
  2. realistically adoptable (cheap + specific).
- What this report is **not**: not a root-cause report, not a blame allocation, not a full threat model.

## 2. Method

<!-- TODO ~1 page -->

### 2.1 Phase list
Ten phases (`data/phases.yaml`), derived from the public timeline. State how the ~17,600 actions were mapped onto ten rows and where the boundaries are debatable.

### 2.2 Controls catalog
Sixteen candidate controls (`data/controls.yaml`). Each carries: category (prevent/detect/contain), external verifiability + how, implementation cost, operating cost, evidence a third party can request. Explain selection rule: TODO (e.g. "must exist as an off-the-shelf configuration in at least one major cloud/K8s distribution").

### 2.3 Scoring cells
`yes` / `partial` / `no` per (phase, control) pair. State the standard of evidence: TODO (e.g. "yes only if the public timeline shows the specific action the control blocks").

### 2.4 Cost scale
Define low / med / high for a lab of Hugging Face's approximate size. TODO — give one anchoring example per level.

### 2.5 Framework cross-walk
Map controls onto CSA AI Controls Matrix (esp. AIS-13 sandboxing) [CSA-AICM], CoSAI incident-response framework [COSAI-IR], NIST SP 800-61r3 [NIST-800-61r3]. One table, no prose.

## 3. The Matrix

<!-- TODO ~1.5 pages. Paste out/matrix.md here after `make matrix`. -->

### 3.1 Headline finding
TODO — one sentence a judge can quote. Candidate shape: "N of 10 phases would have been interrupted by K controls with combined implementation cost X, all externally verifiable."

### 3.2 The table
TODO (generated).

### 3.3 Reading the table
- Phases with no `yes` (uncovered) — what that means.
- Controls that interrupt >= 3 phases — the cheap wins.
- Detection-only rows — why a page-worthy signal is scored differently from a block.

## 4. Cost analysis

<!-- TODO ~1 page -->

- Cost per control, cost per interrupted phase.
- The "minimum sufficient set": smallest set of controls covering every phase at least once; its total cost.
- Where cost is dominated by operating load (allowlist churn, on-call) versus one-time work.
- Sensitivity: which cells, if flipped, change the minimum set.

## 5. Limitations & Dual-Use Considerations (REQUIRED)

<!-- TODO ~1 page. Judges require this section. Do not cut. -->

### 5.1 What this cannot establish
Mirror README "What this cannot establish". Counterfactuals, victim internal state, attacker capability ceiling, coarse costs, detection-to-response gap, phase-list completeness.

### 5.2 Dual-use
- The matrix is also a map of which controls were absent. Argue why publication is net-positive: TODO (public record already contains the attack path; controls named are standard).
- Anything deliberately withheld: TODO (e.g. no exploit specifics beyond what HF published).
- Misuse scenario considered and why it is not enabled by this document: TODO.

### 5.3 Bias and provenance
Sources are the victim's and the vendor's own accounts. Note incentives.

## 6. What a month of follow-up would add

<!-- TODO ~0.5 page -->

- Replay one or two phases in a lab cluster to convert `partial` cells to `yes`/`no`.
- Cost survey with 3–5 labs to replace low/med/high with ranges.
- Third-party attestation template: what a lab would publish so criterion 1 is met by default.
- Extend rows to the ~9-phase granularity HF used, or finer.
- Detection-path study: why correlated alerts did not page.

---

## References

See `CITATIONS.md`.

## Appendix A — Full matrix (HTML)

`out/matrix.html` — generated by `make matrix`.

## Appendix B — Controls catalog

`data/controls.yaml` verbatim.
