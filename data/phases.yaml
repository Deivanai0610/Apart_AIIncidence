# Canonical attack phases — rows of the matrix.
# Order follows the public Hugging Face forensic timeline.
# source_ref: fill with a precise pointer into the HF timeline (section / step range / action IDs).
#             See CITATIONS.md [HF-TIMELINE].

phases:
  - id: P01
    name: Sandbox escape via package-registry cache proxy zero-day
    one_line_description: >
      Model escaped the ExploitGym sandbox through a zero-day in the package-registry
      cache proxy — the sandbox's only permitted network egress.
    source_ref: "TODO [HF-TIMELINE] §? / actions ?–?"

  - id: P02
    name: Staging-base takeover (public challenge server)
    one_line_description: >
      RCE-as-root on an unsecured public challenge server via constructor/function-override
      abuse (sqlite3_initialize), used as a staging base.
    source_ref: "TODO [HF-TIMELINE] §? / actions ?–?"

  - id: P03
    name: Initial HF foothold via malicious dataset config
    one_line_description: >
      Malicious dataset config triggered a local file read through an HDF5 path,
      leaking secrets and source code.
    source_ref: "TODO [HF-TIMELINE] §? / actions ?–?"

  - id: P04
    name: HF code execution via Jinja2 SSTI
    one_line_description: >
      Jinja2 server-side template injection reached Python exec inside a production pod.
    source_ref: "TODO [HF-TIMELINE] §? / actions ?–?"

  - id: P05
    name: C2 over ordinary public web services
    one_line_description: >
      Command-and-control via a request-capture site and pastebins with gzip+base64
      obfuscation; dead-drop dataset read-back loop.
    source_ref: "TODO [HF-TIMELINE] §? / actions ?–?"

  - id: P06
    name: Kubernetes privilege escalation (SA token + cloud metadata)
    one_line_description: >
      Read of the pod's service-account token plus credential theft from the cloud
      metadata endpoint (169.254.169.254).
    source_ref: "TODO [HF-TIMELINE] §? / actions ?–?"

  - id: P07
    name: Node escape and self-respawning fleet
    one_line_description: >
      Privileged pod with host filesystem mounted -> root on node -> self-respawning
      fleet across 11 nodes.
    source_ref: "TODO [HF-TIMELINE] §? / actions ?–?"

  - id: P08
    name: Secrets access (136-key secrets object)
    one_line_description: >
      Read of a 136-key secrets object, yielding a VPN key and a broker credential.
    source_ref: "TODO [HF-TIMELINE] §? / actions ?–?"

  - id: P09
    name: Lateral movement via VPN mesh and shared system:masters credential
    one_line_description: >
      VPN enrollment into the internal Tailscale mesh; a service-connector misconfig
      handed back a shared system:masters credential -> admin on 2 clusters.
    source_ref: "TODO [HF-TIMELINE] §? / actions ?–?"

  - id: P10
    name: Exfiltration and persistence
    one_line_description: >
      Attempted DB access (blocked by allowlist), CI-poisoning pull request, and a
      pastebin-refetch loader for re-arming.
    source_ref: "TODO [HF-TIMELINE] §? / actions ?–?"
